# ============================================================================
# BoxLog Web - 環境変数設定例
# ============================================================================
#
# このファイルをコピーして `.env.local` を作成してください:
#   cp .env.example .env.local
#
# 型定義: src/config/env.ts
#

# ============================================================================
# Node.js 環境
# ============================================================================

# 実行環境 (development | production | test)
# Next.js が自動的に設定するため、通常は設定不要
# NODE_ENV=development

# CI/CD 環境フラグ
# GitHub Actions などで自動的に設定されます
# CI=true


# ============================================================================
# アプリケーション設定
# ============================================================================

# アプリケーションの公開 URL
# 本番環境では必須
# 例: https://boxlog.app
# NEXT_PUBLIC_APP_URL=

# サイトの公開 URL (SEO・OGP用)
# 未設定の場合は NEXT_PUBLIC_APP_URL を使用
# NEXT_PUBLIC_SITE_URL=

# Vercel のデプロイURL
# Vercel環境で自動的に設定されます
# VERCEL_URL=


# ============================================================================
# セキュリティ設定
# ============================================================================

# プライバシー保護モード (normal | strict)
#
# - normal: メールアドレスを部分的にマスク (例: u***@example.com)
# - strict: メールアドレスを完全にマスク (例: ***@***)
#
# デフォルト: normal
PRIVACY_PROTECTION_MODE=normal


# ============================================================================
# 外部サービス - GitHub
# ============================================================================

# GitHub Personal Access Token
#
# コンタクトフォームからの問い合わせを GitHub Issues として作成するために使用
# 本番環境では必須
#
# スコープ: 'repo' (プライベートリポジトリの場合) または 'public_repo'
# 取得方法: https://github.com/settings/tokens/new
#
GITHUB_TOKEN=

# GitHub リポジトリ (owner/repo 形式)
#
# コンタクトフォームの Issue を作成するリポジトリ
# デフォルト: t3-nico/boxlog-web
#
# 例: your-org/your-repo
GITHUB_CONTACT_REPO=t3-nico/boxlog-web


# ============================================================================
# 外部サービス - Upstash (Rate Limiting)
# ============================================================================

# Upstash Redis REST API URL
#
# レート制限機能を使用する場合は設定してください
# 未設定の場合はメモリベースのレート制限にフォールバックします
# （開発環境ではメモリベースで十分です）
#
# 取得方法:
# 1. https://console.upstash.com/ でアカウント作成
# 2. Redis データベースを作成
# 3. REST API セクションから URL と TOKEN を取得
#
UPSTASH_REDIS_REST_URL=

# Upstash Redis REST API Token
#
# UPSTASH_REDIS_REST_URL と両方設定する必要があります
# 片方のみ設定した場合は警告が表示され、メモリベースにフォールバックします
#
UPSTASH_REDIS_REST_TOKEN=


# ============================================================================
# SEO - 検索エンジン検証
# ============================================================================

# Google Search Console 検証コード
#
# Google Search Console でサイトの所有権を確認するためのメタタグ値
# 取得方法: https://search.google.com/search-console
#
# 例: abc123def456ghi789
GOOGLE_SITE_VERIFICATION=

# Yandex Webmaster 検証コード
#
# Yandex Webmaster でサイトの所有権を確認するためのメタタグ値
# 取得方法: https://webmaster.yandex.com/
#
YANDEX_VERIFICATION=

# Yahoo! Search 検証コード
#
# Yahoo! Search でサイトの所有権を確認するためのメタタグ値
#
YAHOO_VERIFICATION=


# ============================================================================
# レート制限設定（参考情報）
# ============================================================================
#
# 以下のレート制限が実装されています:
#
# - コンタクトフォーム: 同一IPから5分間に3回まで
# - 検索API: 同一IPから1分間に30回まで
# - タグAPI: 同一IPから1分間に60回まで
#
# これらの設定は src/lib/rate-limit.ts で変更できます
#


# ============================================================================
# 開発環境セットアップ
# ============================================================================
#
# 最低限の開発環境セットアップ:
#
# 1. このファイルをコピー:
#    cp .env.example .env.local
#
# 2. 必須設定（開発環境では任意）:
#    - GITHUB_TOKEN: コンタクトフォームをテストする場合のみ
#    - UPSTASH_*: レート制限をテストする場合のみ
#
# 3. 開発サーバー起動:
#    npm run dev
#
# 本番環境セットアップ:
#
# 1. Vercel環境変数に以下を設定:
#    - GITHUB_TOKEN (必須)
#    - UPSTASH_REDIS_REST_URL (推奨)
#    - UPSTASH_REDIS_REST_TOKEN (推奨)
#    - NEXT_PUBLIC_APP_URL (自動設定されない場合)
#
# 2. デプロイ:
#    vercel --prod
#
